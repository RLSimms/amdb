<%= form_for(@role) do |f| %>
  <% if @role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@role.errors.count, "error") %> prohibited this role from being saved:</h2>

      <ul>
      <% @role.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :character_name %><br />
    <%= f.text_field :character_name %>
  </div>
  <div class="field">
    <%= f.label :movie_title %><br />
    <%= f.text_field :movie_title, data: {autocomplete_source: autocomplete_movies_path} %>
  </div>
  <div class="field">
    <%= f.label :actor_name %><br />
    <%= f.text_field :actor_name, data: {autocomplete_source: autocomplete_actors_path} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>







